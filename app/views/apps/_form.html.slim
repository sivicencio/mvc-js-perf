= simple_form_for @app, html: {class: 'form-vertical' } do |f|
  = f.error_notification
  = f.input :name, autofocus: true
  = f.input :project_url, autofocus: true
  = f.button :submit, class: 'btn-primary'